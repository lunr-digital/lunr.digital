import { css } from 'styled-components';
import theme from '../utils/theme';

export default css`
    .tac {
        text-align: center;
    }

    .pt-8 {
        padding-top: ${theme('spacing.8')};
    }
`;
